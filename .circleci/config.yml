version: 2.1

jobs:
  lint:
    docker:
      - image: cimg/php:7.3-node
    steps:
      - checkout:
          path: plugins/fse-studio
      - run:
          name: Linting PHP
          working_directory: plugins/fse-studio
          command: |
            npm run installation
            npm run lint:php
      - run:
          name: Integration Testing with phpunit
          working_directory: plugins/fse-studio
          command: |
            npm run test:phpunit
      - run:
          name: Linting JS and CSS
          working_directory: plugins/fse-studio
          command: |
            npm run lint:js
            npm run lint:css
  phpunit:
    machine:
      image: ubuntu-2004:202111-02

    steps:
      - checkout:
          path: plugins/fse-studio
      - run:
          name: PHPUnit Integreation Tests
          working_directory: plugins/fse-studio
          command: |
            npm run installation
            npm run test:php

workflows:
  build:
    jobs:
      - lint
